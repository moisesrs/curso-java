## 🎓 Exercício Prático

Crie uma classe `Aluno` com:
- Atributos privados: `nome`, `matricula`, `nota1`, `nota2`
- Use Lombok para gerar getters e setters
- Crie um método `calcularMedia()` que retorne a média das duas notas
- Crie um método `situacao()` que retorne "Aprovado" se média >= 7, senão "Reprovado"
- **Desafio**: Faça validação no setter das notas (não pode ser negativa nem maior que 10)
